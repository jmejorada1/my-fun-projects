-- backend--add domain to tables. For example 'imdb', 'thought-pattern'.

-- add tests in both projects
-- create UI, in Angular
        - user can log in just by enter email (this will be expanded in future)
        - user will have a left hand frame of all their posts
        - user will have a search bar to search all programs
        - user will have a right hand panel displaying top 5 resources per category

--- add to github

** Frontend-Angular

   X-- move posts section
   X-- better css
   X-- sortable tables (descending first)
   X-- make post column resizable
   X-- make my posts and my replies collaosable
   X-- add "...more" to left panel
   X-- BUG: When i click reply change focus to text box.
   X-- BUG: pane is not updated when adding new replies
   X-- also add a cancel reply button (red)?
   X-- drill down capabilities on side panels
   X-- when user adds a no-bigotry card on a resource, then can you remove previous severity cards from user if user agrees? In other words warn user first, with a floatable window that shows all the cards that will be removed in a clickable list. Allow them to drill down and view each post/reply before agreeing to delete cards, and add the no-bigotry card.

   -- Add datagrid with pagination for search results?
   -- polling or refresh polling?

   -- Scrollable table header?
   -- When a user scrolls down, can you maintain "back to search" and "back to top" links?
   -- make search ajaxable
   -- BUG: on multi text, click reply doesnt scroll down to text box.
   -- Add back/forward buttons

*** IMDB Data ****
   X-- Load imdb data, but ignore when primaryTitle is prefixed with Episode #   

*** Code Changes ****

   -- organize app/api package
        -- Do not hardcode localhost:4200 everywhere. Remove any hardcoded things. parameterize when possible
   
   
   -- restructure code to be easily configurable by domain.
        --- In other words, Can you make the domain and configurations parameterizable through a json file or datastructure/classes that can be processed? For example I want to add an imdb/standard domain that will have a different set of post-categories, for example:
                - skip it
                - It was okay
                - I enjoyed it
                - I loved it, 
        And there will be no severity score to average. Just counts of the above chosen categories for rating a movie.

        Or would it be simpler to create a separate Angular (or React) UI project to handle this new "domain"?

        Do not implement anything. Plan this in a new markdown file in imdb-ui-angular/docs directory

   

   --- LLM Service API???
        -- Add an LLM summarizing the search results

   
** New IMDB domain imdb-standart-reviews
    - skip it
    - It was okay
    - I enjoyed it
    - I loved it, 


** IMPORTANT ***
   * Security check for security vulns (like sql injection, etc.)
   * Test race condition scenarios

** New TODO application:
      - Resource is todo item
      - post-categories:
                Waiting
                In-Progress
                Done
      - Add severity on "Waiting" state
